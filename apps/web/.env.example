# TypeSafe Jev (live UI planning — required, no mock fallback)
# Get your key from https://console.typesafe.ai/settings/keys
TYPESAFE_API_KEY=

# Vercel KV (Rate Limiting, optional — no-op locally without it)
# Automatically populated when you add Vercel KV to your project
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Rate Limiting
RATE_LIMIT_PER_MINUTE=
RATE_LIMIT_PER_DAY=
